"""Training infrastructure.

Modules:
- config.py  : TrainConfig dataclass (hyperparameters, loss weights, checkpointing)
- trainer.py : Trainer class (epoch loop, multi-task loss, early stopping)
"""
